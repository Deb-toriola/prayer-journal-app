# Optional analytics IDs — if absent, scripts are not injected.
NEXT_PUBLIC_GA_ID=
NEXT_PUBLIC_META_PIXEL_ID=

# Optional email delivery for inspection leads (Resend).
# If absent, leads are logged to stdout via the abstracted notify() helper.
RESEND_API_KEY=
INSPECTION_NOTIFY_EMAIL=
